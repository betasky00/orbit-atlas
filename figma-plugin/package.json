{
    "name": "orbit-atlas-figma-plugin",
    "version": "1.0.0",
    "description": "Figma plugin that injects Orbit Atlas carousel content (texts + images) into named layers, and notifies the workflow once validated.",
    "scripts": {
          "build": "tsc -p tsconfig.json",
          "watch": "tsc -p tsconfig.json --watch"
    },
    "devDependencies": {
          "@figma/plugin-typings": "^1.100.0",
          "typescript": "^5.6.0"
    }
}
